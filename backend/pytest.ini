[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ; SQLite no tiene tipo decimal nativo; en tests no afecta lo que se verifica.
    ignore:.*does \*not\* support Decimal objects natively.*:Warning
