# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
.pytest_cache/
backend/tests/test.db
# SQLite deja estos al lado mientras corre la suite; con la suite en curso
# aparecen como archivos nuevos y se cuelan en un `git add -A`.
backend/tests/test.db-journal
backend/tests/test.db-wal
backend/tests/test.db-shm

# Environment
.env
*.env.local
# Lleva la contraseña de Postgres y la clave que firma las sesiones. El que sí
# va al repo es `.env.production.example`, que no matchea con esta línea.
.env.production

# Dumps de la base: el padrón de socios entero en texto plano.
backups/

# Node
node_modules/
dist/
.vite/
coverage/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
.dockerignore
